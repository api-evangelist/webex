{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/SelectiveRejectCallSource",
  "title": "SelectiveRejectCallSource",
  "type": "string",
  "enum": [
    "ALL_NUMBERS",
    "SPECIFIC_NUMBERS",
    "FORWARDED"
  ],
  "description": " * `ALL_NUMBERS` - Selective reject criteria applies for all incoming numbers.\n * `SPECIFIC_NUMBERS` - Selective reject criteria applies for calls from specific numbers.\n * `FORWARDED` - Selective reject criteria applies for all forwarded calls.\n"
}