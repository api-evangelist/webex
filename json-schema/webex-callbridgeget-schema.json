{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/CallBridgeGet",
  "title": "CallBridgeGet",
  "type": "object",
  "required": [
    "warningToneEnabled"
  ],
  "properties": {
    "warningToneEnabled": {
      "type": "boolean",
      "description": "`true` if the Call Bridge Warning Tone feature is enabled."
    }
  }
}