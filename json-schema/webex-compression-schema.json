{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/Compression",
  "title": "Compression",
  "type": "string",
  "enum": [
    "ON",
    "OFF"
  ],
  "description": " * `ON` - Minimize data use during compression.\n * `OFF` - Ignore data use during compression.\n"
}