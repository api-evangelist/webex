{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/AnonymousCallRejectionPatch",
  "title": "AnonymousCallRejectionPatch",
  "type": "object",
  "required": [
    "enabled"
  ],
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "`true` if the Anonymous Call Rejection feature is enabled."
    }
  }
}