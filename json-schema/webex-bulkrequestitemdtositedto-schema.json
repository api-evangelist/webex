{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/BulkRequestItemDTOSiteDTO",
  "title": "BulkRequestItemDTOSiteDTO",
  "type": "object",
  "properties": {
    "itemIdentifier": {
      "type": "integer",
      "description": "Unique item identifier for a bulk operation.",
      "format": "int32",
      "example": 10
    },
    "item": {
      "$ref": "#/components/schemas/SiteDTO"
    },
    "requestAction": {
      "type": "string",
      "description": "Identifier for action type. Possible values can be SAVE and DELETE.",
      "example": "SAVE"
    }
  }
}