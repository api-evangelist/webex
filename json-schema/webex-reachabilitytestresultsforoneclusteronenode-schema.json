{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/ReachabilityTestResultsforoneclusteronenode",
  "title": "ReachabilityTestResultsforoneclusteronenode",
  "type": "object",
  "properties": {
    "clusters": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/ReachabilityTestResultsforonecluster"
      },
      "description": "List of Video Mesh clusters."
    }
  }
}