{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/AgentActivitiesApiResponse",
  "title": "AgentActivitiesApiResponse",
  "required": [
    "data",
    "meta"
  ],
  "type": "object",
  "properties": {
    "meta": {
      "description": "Response metadata.",
      "$ref": "#/components/schemas/Meta"
    },
    "data": {
      "type": "array",
      "description": "List of agent activities retrieved according to query parameters.",
      "items": {
        "$ref": "#/components/schemas/AgentActivity"
      }
    }
  },
  "description": "Response wrapper for agent activities."
}