{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/DialPatternValidationStatus",
  "title": "DialPatternValidationStatus",
  "type": "string",
  "enum": [
    "ERRORS",
    "OK"
  ],
  "description": " * `ERRORS` - In case one or more dial pattern validation failed\n * `OK` - If all the patterns are validated successfully\n"
}